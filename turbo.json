{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["lib/mjs/**","lib/cjs/**"]
    },
    "test": {
      "outputs": []
    }
  }
}
